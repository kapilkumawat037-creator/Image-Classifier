# Image-Classifier
I built a handwritten digit classifier using a CNN trained on the MNIST dataset. It uses TensorFlow and Keras under the hood, with a few convolutional layers to pick up on patterns in the images. The model trains in about 5 epochs and does a pretty solid job recognizing digits from 0 to 9.
